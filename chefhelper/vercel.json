{
  "buildCommand": "pip install -r requirements.txt",
  "installCommand": "pip install -r requirements.txt",
  "outputDirectory": "app",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
